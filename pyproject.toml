[project]
name = "parts-catalog-co"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.20",
]

[tool.ruff]
# Ruff defaults (line-length 88, lint E4/E7/E9/F). F811 catches shadowed
# test defs the verify gate would otherwise miss (Day-1 A4).
